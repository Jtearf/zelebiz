import React from 'react';
import ProfileScreen from '../screens/ProfileScreen';

// Profile tab screen that loads the ProfileScreen component
export default function ProfileTab() {
  return <ProfileScreen />;
}
